services:
  - type: web
    name: eyed
    runtime: python
    rootDir: Eye-D
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --app-dir eyed_backend --host 0.0.0.0 --port $PORT
    envVars:
      - key: PORT
        value: "8000"
